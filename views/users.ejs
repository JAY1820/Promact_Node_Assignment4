<!DOCTYPE html>
<html>
<head>
    <title>Users</title>
</head>
<body>
    <ul>
        <% for(let i=0; i<users.length; i++) { %>
            <li><%= users[i] %></li>
        <% } %>
    </ul>
</body>
</html>
