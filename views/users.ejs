<!DOCTYPE html>
<html>
<head>
    <title>Users</title>
    <!-- Add some CSS for styling -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <!-- Unordered list of usernames -->
    <ul>
        <% for(let i=0; i<users.length; i++) { %>
            <li><%= users[i] %></li>
        <% } %>
    </ul>
</body>
</html>
